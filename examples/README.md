# Examples

Examples are recipes that demonstrate how to use **SerenadeFlow** with different *plugins*, *data sources*, and *configurations*.

## Running Examples

```bash
PYTHONPATH=. python3 examples/read_and_export.py
```

Replace `read_and_export.py` with the name of the example you want to run.